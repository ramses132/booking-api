"""Setuptools package definition."""

from setuptools import find_packages, setup

setup(
    name="booking-api",
    version="0.0.0",
    author="ramses132",
    description="Booking API project for admission test",
    url="ramses.booking-api.com",
    packages=find_packages(),
)
